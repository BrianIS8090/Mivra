# S3 Uploads Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить загрузку картинок и документов прямо из редактора в S3-совместимый bucket с автоматической вставкой публичной ссылки в markdown.

**Architecture:** Frontend triggers (drag&drop, paste, button) → React-хук `useS3Upload` → IPC → Rust-команды → `rust-s3` клиент → bucket. Secret хранится в OS keyring; остальные настройки в `settings.json`. specta автоматически генерирует TS-типы из Rust-структур.

**Tech Stack:** Rust (`rust-s3`, `keyring`, `uuid`, `wiremock` для тестов) + Tauri 2 + React 19 + TypeScript + Zustand + Vitest.

**Spec:** [docs/superpowers/specs/2026-04-29-s3-uploads-design.md](../specs/2026-04-29-s3-uploads-design.md)

---

## File Structure

**Новые файлы:**
- `src-tauri/src/s3.rs` — Rust-модуль: `S3Config`, sanitize, key builder, public URL derive, upload core с retry-логикой.
- `src/components/Toast/Toast.tsx` + `toast.css` — компонент уведомления.
- `src/components/Toast/ToastContainer.tsx` — портал-контейнер очереди.
- `src/hooks/useToast.ts` — публичный хук show/update/dismiss.
- `src/stores/toastStore.ts` — Zustand-стор очереди toast'ов.
- `src/components/S3Settings/S3SettingsDialog.tsx` + `s3-settings.css` — модал настроек.
- `src/hooks/useS3Upload.ts` — публичный хук с upload-логикой.

**Модифицируемые:**
- `src-tauri/Cargo.toml` — deps.
- `src-tauri/src/commands.rs` — `Settings.s3`, S3 IPC-команды.
- `src-tauri/src/lib.rs` — регистрация команд в specta builder.
- `src/bindings.ts` — авто-перегенерация.
- `src/utils/tauri.ts` — обёртки над новыми commands.*.
- `src/types/index.ts` — реэкспорт `S3Config` из bindings.
- `src/stores/appStore.ts` — `s3` в state, `setS3Config` action.
- `src/components/Toolbar/Toolbar.tsx` — кнопка S3 левее Help.
- `src/components/Editor/Editor.tsx` — drop/paste handlers.
- `src/hooks/useMarkdownActions.ts` — расширение `insertAssetAction`.
- `src/i18n/ru.ts` + `en.ts` — новые строки.

---

## Task 1: Cargo deps + s3.rs skeleton

**Files:**
- Modify: `src-tauri/Cargo.toml`
- Create: `src-tauri/src/s3.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: Добавить зависимости в Cargo.toml**

Дописать в `[dependencies]`:

```toml
rust-s3 = { version = "0.35", default-features = false, features = ["tokio-rustls-tls"] }
keyring = "3"
uuid = { version = "1", features = ["v4"] }
mime_guess = "2"
```

И блок `[dev-dependencies]`:

```toml
[dev-dependencies]
wiremock = "0.6"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
```

- [ ] **Step 2: Создать пустой модуль s3.rs**

```rust
// src-tauri/src/s3.rs
use serde::{Deserialize, Serialize};
use specta::Type;

#[derive(Serialize, Deserialize, Clone, Debug, Type)]
pub struct S3Config {
  pub endpoint: String,
  pub region: String,
  pub bucket: String,
  pub access_key_id: String,
  pub public_url_prefix: Option<String>,
  pub path_prefix: Option<String>,
}
```

- [ ] **Step 3: Зарегистрировать модуль в lib.rs**

В `src-tauri/src/lib.rs` после `pub mod commands;` добавить:

```rust
pub mod s3;
```

- [ ] **Step 4: Проверить, что cargo принимает новые deps**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Expected: успешная компиляция, новые crates загружены.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/s3.rs src-tauri/src/lib.rs
git commit -m "feat(s3): cargo deps + s3.rs module + S3Config struct"
```

---

## Task 2: Sanitize filename + build_key + derive_public_url

**Files:**
- Modify: `src-tauri/src/s3.rs`

- [ ] **Step 1: Написать failing-тесты в s3.rs**

Дописать в конец `s3.rs`:

```rust
#[cfg(test)]
mod tests {
  use super::*;

  #[test]
  fn sanitize_replaces_dangerous_chars() {
    assert_eq!(sanitize_filename("foo/bar.png"), "foo_bar.png");
    assert_eq!(sanitize_filename("a\\b<c>:d|e?f*g\"h"), "a_b_c_d_e_f_g_h");
  }

  #[test]
  fn sanitize_keeps_unicode() {
    assert_eq!(sanitize_filename("скриншот.png"), "скриншот.png");
    assert_eq!(sanitize_filename("file with spaces.png"), "file with spaces.png");
  }

  #[test]
  fn sanitize_truncates_to_100_chars() {
    let long_name = "a".repeat(200);
    let result = sanitize_filename(&long_name);
    assert_eq!(result.chars().count(), 100);
  }

  #[test]
  fn build_key_with_path_prefix() {
    let key = build_key(Some("mivra/"), "abc-123", "image.png");
    assert_eq!(key, "mivra/abc-123-image.png");
  }

  #[test]
  fn build_key_normalizes_trailing_slash_in_prefix() {
    let key1 = build_key(Some("mivra"), "abc-123", "image.png");
    let key2 = build_key(Some("mivra/"), "abc-123", "image.png");
    assert_eq!(key1, key2);
    assert_eq!(key1, "mivra/abc-123-image.png");
  }

  #[test]
  fn build_key_without_path_prefix() {
    let key = build_key(None, "abc-123", "image.png");
    assert_eq!(key, "abc-123-image.png");
  }

  #[test]
  fn derive_public_url_with_override() {
    let config = S3Config {
      endpoint: "https://storage.yandexcloud.net".into(),
      region: "ru-central1".into(),
      bucket: "my-bucket".into(),
      access_key_id: "id".into(),
      public_url_prefix: Some("https://cdn.example.com/".into()),
      path_prefix: None,
    };
    assert_eq!(derive_public_url(&config, "abc-foo.png"), "https://cdn.example.com/abc-foo.png");
  }

  #[test]
  fn derive_public_url_without_override_uses_endpoint_path_style() {
    let config = S3Config {
      endpoint: "https://storage.yandexcloud.net".into(),
      region: "ru-central1".into(),
      bucket: "my-bucket".into(),
      access_key_id: "id".into(),
      public_url_prefix: None,
      path_prefix: None,
    };
    assert_eq!(
      derive_public_url(&config, "abc-foo.png"),
      "https://storage.yandexcloud.net/my-bucket/abc-foo.png"
    );
  }

  #[test]
  fn derive_public_url_handles_double_slashes() {
    let config = S3Config {
      endpoint: "https://storage.yandexcloud.net/".into(),
      region: "ru-central1".into(),
      bucket: "my-bucket".into(),
      access_key_id: "id".into(),
      public_url_prefix: Some("https://cdn.example.com/".into()),
      path_prefix: None,
    };
    assert_eq!(derive_public_url(&config, "abc-foo.png"), "https://cdn.example.com/abc-foo.png");
  }
}
```

- [ ] **Step 2: Запустить тесты — должны упасть**

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3`
Expected: 8 тестов FAIL, причина «cannot find function `sanitize_filename`/`build_key`/`derive_public_url`».

- [ ] **Step 3: Имплементировать функции в s3.rs**

Дописать перед блоком `#[cfg(test)]`:

```rust
const DANGEROUS_CHARS: &[char] = &['/', '\\', '\0', '<', '>', ':', '"', '|', '?', '*'];
const MAX_FILENAME_LEN: usize = 100;

pub fn sanitize_filename(name: &str) -> String {
  let cleaned: String = name
    .chars()
    .map(|c| if DANGEROUS_CHARS.contains(&c) { '_' } else { c })
    .collect();
  cleaned.chars().take(MAX_FILENAME_LEN).collect()
}

pub fn build_key(path_prefix: Option<&str>, uuid: &str, sanitized_filename: &str) -> String {
  match path_prefix {
    Some(prefix) if !prefix.is_empty() => {
      let prefix = prefix.trim_end_matches('/');
      format!("{}/{}-{}", prefix, uuid, sanitized_filename)
    }
    _ => format!("{}-{}", uuid, sanitized_filename),
  }
}

pub fn derive_public_url(config: &S3Config, key: &str) -> String {
  let base = match &config.public_url_prefix {
    Some(prefix) if !prefix.is_empty() => prefix.trim_end_matches('/').to_string(),
    _ => format!(
      "{}/{}",
      config.endpoint.trim_end_matches('/'),
      config.bucket
    ),
  };
  format!("{}/{}", base, key)
}
```

- [ ] **Step 4: Запустить тесты — должны пройти**

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3`
Expected: 8 PASS.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/s3.rs
git commit -m "feat(s3): sanitize_filename + build_key + derive_public_url"
```

---

## Task 3: Secret management через keyring

**Files:**
- Modify: `src-tauri/src/s3.rs`

- [ ] **Step 1: Добавить тесты secret-функций**

Дополнить `mod tests` в `s3.rs`:

```rust
  // Тесты используют уникальный username, чтобы не конфликтовать с реальным
  // секретом пользователя при локальном запуске.
  fn test_username() -> String {
    format!("test-{}", uuid::Uuid::new_v4())
  }

  #[test]
  fn keyring_set_get_delete_roundtrip() {
    let username = test_username();
    set_secret_raw(&username, "my_secret").expect("set");
    assert_eq!(get_secret_raw(&username).expect("get"), "my_secret");
    assert!(secret_exists_raw(&username).expect("exists check"));
    delete_secret_raw(&username).expect("delete");
    assert!(!secret_exists_raw(&username).expect("exists check after delete"));
  }

  #[test]
  fn keyring_get_missing_returns_error() {
    let username = test_username();
    assert!(get_secret_raw(&username).is_err());
  }

  #[test]
  fn keyring_delete_missing_does_not_error() {
    let username = test_username();
    // Первое удаление пустого слота — допустимо вернуть Ok или специфичный Err;
    // важно, чтобы не паниковало.
    let _ = delete_secret_raw(&username);
  }
```

- [ ] **Step 2: Запустить — fail на отсутствующих функциях**

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3::tests::keyring`
Expected: FAIL «cannot find function `set_secret_raw`».

- [ ] **Step 3: Имплементировать функции keyring**

В `s3.rs` перед `#[cfg(test)]`:

```rust
const KEYRING_SERVICE: &str = "Mivra";
const KEYRING_USERNAME: &str = "s3_secret_access_key";

fn keyring_entry(username: &str) -> Result<keyring::Entry, String> {
  keyring::Entry::new(KEYRING_SERVICE, username)
    .map_err(|e| format!("Не удалось создать запись keyring: {}", e))
}

pub fn set_secret_raw(username: &str, secret: &str) -> Result<(), String> {
  let entry = keyring_entry(username)?;
  entry
    .set_password(secret)
    .map_err(|e| format!("Не удалось сохранить секрет: {}", e))
}

pub fn get_secret_raw(username: &str) -> Result<String, String> {
  let entry = keyring_entry(username)?;
  entry
    .get_password()
    .map_err(|e| format!("Не удалось прочитать секрет: {}", e))
}

pub fn delete_secret_raw(username: &str) -> Result<(), String> {
  let entry = keyring_entry(username)?;
  entry
    .delete_credential()
    .map_err(|e| format!("Не удалось удалить секрет: {}", e))
}

pub fn secret_exists_raw(username: &str) -> Result<bool, String> {
  let entry = keyring_entry(username)?;
  match entry.get_password() {
    Ok(_) => Ok(true),
    Err(keyring::Error::NoEntry) => Ok(false),
    Err(e) => Err(format!("Ошибка проверки keyring: {}", e)),
  }
}

pub fn set_secret(secret: &str) -> Result<(), String> {
  set_secret_raw(KEYRING_USERNAME, secret)
}

pub fn get_secret() -> Result<String, String> {
  get_secret_raw(KEYRING_USERNAME)
}

pub fn delete_secret() -> Result<(), String> {
  delete_secret_raw(KEYRING_USERNAME)
}

pub fn secret_exists() -> Result<bool, String> {
  secret_exists_raw(KEYRING_USERNAME)
}
```

- [ ] **Step 4: Запустить тесты**

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3::tests::keyring`
Expected: 3 PASS.

> Если тесты падают на CI без HEAD-доступа к keyring (например, в headless Linux runner): на Windows в локальной разработке всё работает. Headless-CI — отдельная задача (mock keyring), но в текущем CI Mivra пока что Windows-only, и keyring доступен.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/s3.rs
git commit -m "feat(s3): keyring set/get/delete/exists для access key secret"
```

---

## Task 4: Upload core с retry — wiremock тесты

**Files:**
- Modify: `src-tauri/src/s3.rs`

- [ ] **Step 1: Написать integration-тест с wiremock**

Дополнить `mod tests` в `s3.rs`:

```rust
  use wiremock::matchers::{method, path};
  use wiremock::{Mock, MockServer, ResponseTemplate};

  fn test_config(endpoint: String) -> S3Config {
    S3Config {
      endpoint,
      region: "us-east-1".to_string(),
      bucket: "test-bucket".to_string(),
      access_key_id: "test-key".to_string(),
      public_url_prefix: None,
      path_prefix: None,
    }
  }

  #[tokio::test]
  async fn upload_bytes_succeeds_on_200() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
      .respond_with(ResponseTemplate::new(200))
      .mount(&server)
      .await;

    let config = test_config(server.uri());
    let url = upload_bytes_with_secret(&config, "test_secret", b"hello".to_vec(), "test.txt")
      .await
      .expect("upload should succeed");
    assert!(url.ends_with("test.txt"));
    assert!(url.contains("test-bucket"));
  }

  #[tokio::test]
  async fn upload_bytes_fails_on_403_without_retry() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
      .respond_with(ResponseTemplate::new(403))
      .expect(1)
      .mount(&server)
      .await;

    let config = test_config(server.uri());
    let result = upload_bytes_with_secret(&config, "secret", b"x".to_vec(), "f.txt").await;
    assert!(result.is_err(), "403 should fail");
  }

  #[tokio::test]
  async fn upload_bytes_retries_on_500_then_succeeds() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
      .respond_with(ResponseTemplate::new(500))
      .up_to_n_times(2)
      .mount(&server)
      .await;
    Mock::given(method("PUT"))
      .respond_with(ResponseTemplate::new(200))
      .mount(&server)
      .await;

    let config = test_config(server.uri());
    let result = upload_bytes_with_secret(&config, "secret", b"x".to_vec(), "f.txt").await;
    assert!(result.is_ok(), "should succeed after retry");
  }

  #[tokio::test]
  async fn upload_bytes_fails_after_three_500s() {
    let server = MockServer::start().await;
    Mock::given(method("PUT"))
      .respond_with(ResponseTemplate::new(500))
      .expect(3)
      .mount(&server)
      .await;

    let config = test_config(server.uri());
    let result = upload_bytes_with_secret(&config, "secret", b"x".to_vec(), "f.txt").await;
    assert!(result.is_err(), "should fail after 3 retries");
  }
```

- [ ] **Step 2: Запустить — должны fail**

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3::tests::upload`
Expected: FAIL «cannot find function `upload_bytes_with_secret`».

- [ ] **Step 3: Имплементировать upload core**

В `s3.rs` перед `#[cfg(test)]`:

```rust
use s3::bucket::Bucket;
use s3::creds::Credentials;
use s3::region::Region;
use std::time::Duration;

const RETRY_DELAYS_MS: &[u64] = &[1000, 2000, 4000];
const MAX_ATTEMPTS: usize = 3;

fn build_bucket(config: &S3Config, secret: &str) -> Result<Bucket, String> {
  let credentials = Credentials::new(
    Some(&config.access_key_id),
    Some(secret),
    None,
    None,
    None,
  )
  .map_err(|e| format!("Ошибка credentials: {}", e))?;

  let region = Region::Custom {
    region: config.region.clone(),
    endpoint: config.endpoint.clone(),
  };

  let bucket = Bucket::new(&config.bucket, region, credentials)
    .map_err(|e| format!("Ошибка инициализации bucket: {}", e))?
    .with_path_style();

  Ok(*bucket)
}

fn classify_error_status(status: u16) -> bool {
  // true → стоит ретраить
  status >= 500 || status == 0
}

pub async fn upload_bytes_with_secret(
  config: &S3Config,
  secret: &str,
  bytes: Vec<u8>,
  original_filename: &str,
) -> Result<String, String> {
  let bucket = build_bucket(config, secret)?;
  let sanitized = sanitize_filename(original_filename);
  let id = uuid::Uuid::new_v4().to_string();
  let key = build_key(config.path_prefix.as_deref(), &id, &sanitized);

  let content_type = mime_guess::from_path(&sanitized)
    .first_or_octet_stream()
    .to_string();

  let mut last_err: Option<String> = None;
  for attempt in 0..MAX_ATTEMPTS {
    if attempt > 0 {
      tokio::time::sleep(Duration::from_millis(RETRY_DELAYS_MS[attempt - 1])).await;
    }

    let result = bucket
      .put_object_with_content_type(&key, &bytes, &content_type)
      .await;

    match result {
      Ok(response) => {
        let status = response.status_code();
        if (200..300).contains(&status) {
          return Ok(derive_public_url(config, &key));
        }
        if !classify_error_status(status) {
          return Err(format!("HTTP {}: {}", status, String::from_utf8_lossy(response.bytes())));
        }
        last_err = Some(format!("HTTP {}", status));
      }
      Err(e) => {
        last_err = Some(format!("network: {}", e));
      }
    }
  }

  Err(last_err.unwrap_or_else(|| "Неизвестная ошибка загрузки".to_string()))
}

pub async fn upload_file_with_secret(
  config: &S3Config,
  secret: &str,
  local_path: &str,
  original_filename: &str,
) -> Result<String, String> {
  let bytes = std::fs::read(local_path).map_err(|e| format!("Ошибка чтения файла: {}", e))?;
  upload_bytes_with_secret(config, secret, bytes, original_filename).await
}
```

- [ ] **Step 4: Запустить тесты**

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3::tests::upload`
Expected: 4 PASS. Может занять до 15 секунд из-за retry-задержек в одном тесте.

> **Если** тест `upload_bytes_retries_on_500_then_succeeds` тайаутится — это из-за реальных sleep'ов в retry. Допустимо сократить `RETRY_DELAYS_MS` до `[10, 20, 40]` через `#[cfg(test)]` константу. Если так делать — добавить версии:
>
> ```rust
> #[cfg(not(test))]
> const RETRY_DELAYS_MS: &[u64] = &[1000, 2000, 4000];
> #[cfg(test)]
> const RETRY_DELAYS_MS: &[u64] = &[10, 20, 40];
> ```

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/s3.rs
git commit -m "feat(s3): upload_bytes_with_secret + upload_file_with_secret + retry"
```

---

## Task 5: Test connection функция

**Files:**
- Modify: `src-tauri/src/s3.rs`

- [ ] **Step 1: Тест connection-checker через wiremock**

Дополнить `mod tests`:

```rust
  use wiremock::matchers::any;

  #[tokio::test]
  async fn test_connection_succeeds_on_200() {
    let server = MockServer::start().await;
    Mock::given(method("HEAD"))
      .respond_with(ResponseTemplate::new(200))
      .mount(&server)
      .await;

    let config = test_config(server.uri());
    assert!(test_connection_with_secret(&config, "secret").await.is_ok());
  }

  #[tokio::test]
  async fn test_connection_fails_on_403() {
    let server = MockServer::start().await;
    Mock::given(any())
      .respond_with(ResponseTemplate::new(403))
      .mount(&server)
      .await;

    let config = test_config(server.uri());
    let err = test_connection_with_secret(&config, "secret").await.unwrap_err();
    assert!(err.contains("403") || err.contains("auth"), "got: {}", err);
  }

  #[tokio::test]
  async fn test_connection_fails_on_404() {
    let server = MockServer::start().await;
    Mock::given(any())
      .respond_with(ResponseTemplate::new(404))
      .mount(&server)
      .await;

    let config = test_config(server.uri());
    let err = test_connection_with_secret(&config, "secret").await.unwrap_err();
    assert!(err.contains("404") || err.contains("not_found"), "got: {}", err);
  }
```

- [ ] **Step 2: Запустить — fail**

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3::tests::test_connection`
Expected: FAIL «cannot find function `test_connection_with_secret`».

- [ ] **Step 3: Имплементировать**

В `s3.rs` перед `#[cfg(test)]`:

```rust
pub async fn test_connection_with_secret(config: &S3Config, secret: &str) -> Result<(), String> {
  let bucket = build_bucket(config, secret)?;

  match bucket.head_object("/").await {
    Ok((_, status)) if (200..300).contains(&status) => Ok(()),
    Ok((_, 404)) => Err("bucket_not_found: bucket не существует или нет прав".to_string()),
    Ok((_, 401)) | Ok((_, 403)) => {
      Err("auth_failed: проверьте Access Key и права на bucket".to_string())
    }
    Ok((_, status)) => Err(format!("HTTP {}", status)),
    Err(e) => Err(format!("network_unreachable: {}", e)),
  }
}
```

- [ ] **Step 4: Запустить — pass**

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3::tests::test_connection`
Expected: 3 PASS.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/s3.rs
git commit -m "feat(s3): test_connection_with_secret для проверки настроек"
```

---

## Task 6: Tauri IPC commands + Settings.s3 + bindings

**Files:**
- Modify: `src-tauri/src/commands.rs`
- Modify: `src-tauri/src/lib.rs`
- Auto-regen: `src/bindings.ts`

- [ ] **Step 1: Расширить Settings**

В `src-tauri/src/commands.rs` обновить структуру `Settings` и `Default`:

```rust
use crate::s3::S3Config;

#[derive(Serialize, Deserialize, Clone, Debug, Type)]
pub struct Settings {
  // existing fields
  #[serde(default = "default_font_family")]
  pub font_family: String,
  #[serde(default = "default_font_size")]
  pub font_size: f32,
  #[serde(default = "default_theme")]
  pub theme: String,
  #[serde(default = "default_language")]
  pub language: String,
  #[serde(default)]
  pub recent_files: Vec<String>,
  #[serde(default = "default_page_width")]
  pub page_width: f32,
  // new
  #[serde(default)]
  pub s3: Option<S3Config>,
}

impl Default for Settings {
  fn default() -> Self {
    Settings {
      font_family: default_font_family(),
      font_size: default_font_size(),
      theme: default_theme(),
      language: default_language(),
      recent_files: Vec::new(),
      page_width: default_page_width(),
      s3: None,
    }
  }
}
```

- [ ] **Step 2: Добавить S3-команды в commands.rs**

Дописать в конец `src-tauri/src/commands.rs`:

```rust
use crate::s3;

#[tauri::command]
#[specta::specta]
pub async fn s3_set_secret(secret: String) -> Result<(), String> {
  s3::set_secret(&secret)
}

#[tauri::command]
#[specta::specta]
pub async fn s3_clear_secret() -> Result<(), String> {
  s3::delete_secret()
}

#[tauri::command]
#[specta::specta]
pub async fn s3_secret_exists() -> Result<bool, String> {
  s3::secret_exists()
}

#[tauri::command]
#[specta::specta]
pub async fn s3_test_connection(config: S3Config) -> Result<(), String> {
  let secret = s3::get_secret().map_err(|_| "secret_not_set".to_string())?;
  s3::test_connection_with_secret(&config, &secret).await
}

#[tauri::command]
#[specta::specta]
pub async fn s3_upload_file(
  local_path: String,
  original_filename: String,
  config: S3Config,
) -> Result<String, String> {
  let secret = s3::get_secret().map_err(|_| "secret_not_set".to_string())?;
  s3::upload_file_with_secret(&config, &secret, &local_path, &original_filename).await
}

#[tauri::command]
#[specta::specta]
pub async fn s3_upload_bytes(
  bytes: Vec<u8>,
  original_filename: String,
  config: S3Config,
) -> Result<String, String> {
  let secret = s3::get_secret().map_err(|_| "secret_not_set".to_string())?;
  s3::upload_bytes_with_secret(&config, &secret, bytes, &original_filename).await
}
```

- [ ] **Step 3: Регистрация команд в lib.rs**

В `src-tauri/src/lib.rs` обновить imports и `build_specta_builder`:

```rust
use commands::{
  get_recent_files, open_file, read_file, read_settings, save_file, save_file_as,
  write_settings,
  // new
  s3_set_secret, s3_clear_secret, s3_secret_exists, s3_test_connection,
  s3_upload_file, s3_upload_bytes,
};

pub fn build_specta_builder() -> tauri_specta::Builder<tauri::Wry> {
  tauri_specta::Builder::<tauri::Wry>::new()
    .commands(tauri_specta::collect_commands![
      open_file,
      save_file,
      save_file_as,
      read_settings,
      write_settings,
      get_recent_files,
      read_file,
      get_pending_file,
      // new
      s3_set_secret,
      s3_clear_secret,
      s3_secret_exists,
      s3_test_connection,
      s3_upload_file,
      s3_upload_bytes,
    ])
}
```

- [ ] **Step 4: Перегенерировать bindings.ts**

Run: `npm run gen:types`
Expected: вывод `✓ Сгенерирован src/bindings.ts`. Проверить, что в `src/bindings.ts` появились функции `s3SetSecret`, `s3ClearSecret`, `s3SecretExists`, `s3TestConnection`, `s3UploadFile`, `s3UploadBytes` и тип `S3Config`.

- [ ] **Step 5: Cargo check + clippy**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Run: `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings`
Expected: оба зелёные.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/commands.rs src-tauri/src/lib.rs src/bindings.ts
git commit -m "feat(s3): IPC команды + Settings.s3 + регенерация bindings"
```

---

## Task 7: TypeScript обёртки в utils/tauri.ts

**Files:**
- Modify: `src/utils/tauri.ts`
- Modify: `src/types/index.ts`

- [ ] **Step 1: Дополнить utils/tauri.ts**

В конец `src/utils/tauri.ts`:

```ts
import type { S3Config } from '../bindings';

export type { S3Config };

export async function s3SetSecret(secret: string): Promise<void> {
  await unwrap(commands.s3SetSecret(secret));
}

export async function s3ClearSecret(): Promise<void> {
  await unwrap(commands.s3ClearSecret());
}

export async function s3SecretExists(): Promise<boolean> {
  return unwrap(commands.s3SecretExists());
}

export async function s3TestConnection(config: S3Config): Promise<void> {
  await unwrap(commands.s3TestConnection(config));
}

export async function s3UploadFile(
  localPath: string,
  originalFilename: string,
  config: S3Config,
): Promise<string> {
  return unwrap(commands.s3UploadFile(localPath, originalFilename, config));
}

export async function s3UploadBytes(
  bytes: Uint8Array,
  originalFilename: string,
  config: S3Config,
): Promise<string> {
  // Tauri 2 принимает Uint8Array как Vec<u8> через числовой массив
  return unwrap(commands.s3UploadBytes(Array.from(bytes), originalFilename, config));
}
```

- [ ] **Step 2: Реэкспорт в types/index.ts**

В `src/types/index.ts` обновить блок reexport:

```ts
import type { Settings, FileData, S3Config } from '../bindings';

export type { Settings, FileData, S3Config };
```

- [ ] **Step 3: Проверить tsc**

Run: `npx tsc --noEmit`
Expected: успешно.

- [ ] **Step 4: Commit**

```bash
git add src/utils/tauri.ts src/types/index.ts
git commit -m "feat(s3): TS-обёртки s3SetSecret/s3UploadFile/etc + реэкспорт типов"
```

---

## Task 8: Toast infrastructure (стор + хук)

**Files:**
- Create: `src/stores/toastStore.ts`
- Create: `src/hooks/useToast.ts`
- Create: `src/test/toast.test.ts`

- [ ] **Step 1: Failing-тесты для toastStore**

`src/test/toast.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { useToastStore } from '../stores/toastStore';

describe('toastStore', () => {
  beforeEach(() => {
    useToastStore.setState({ toasts: [] });
  });

  it('show добавляет toast и возвращает id', () => {
    const id = useToastStore.getState().show('Загружаю', 'loading');
    const toasts = useToastStore.getState().toasts;
    expect(toasts).toHaveLength(1);
    expect(toasts[0].id).toBe(id);
    expect(toasts[0].message).toBe('Загружаю');
    expect(toasts[0].type).toBe('loading');
  });

  it('update меняет существующий toast по id', () => {
    const id = useToastStore.getState().show('Загружаю', 'loading');
    useToastStore.getState().update(id, 'Готово', 'success');
    const t = useToastStore.getState().toasts[0];
    expect(t.message).toBe('Готово');
    expect(t.type).toBe('success');
  });

  it('dismiss удаляет toast', () => {
    const id = useToastStore.getState().show('msg', 'info');
    useToastStore.getState().dismiss(id);
    expect(useToastStore.getState().toasts).toHaveLength(0);
  });

  it('show ограничен max=5 одновременных', () => {
    const store = useToastStore.getState();
    for (let i = 0; i < 7; i++) store.show(`t${i}`, 'info');
    expect(useToastStore.getState().toasts).toHaveLength(5);
    // Старейшие удалены
    expect(useToastStore.getState().toasts[0].message).toBe('t2');
  });
});
```

- [ ] **Step 2: Запустить — fail**

Run: `npm test -- src/test/toast.test.ts`
Expected: FAIL «Cannot find module».

- [ ] **Step 3: Создать toastStore.ts**

```ts
// src/stores/toastStore.ts
import { create } from 'zustand';

export type ToastType = 'loading' | 'success' | 'error' | 'info';
export type ToastId = string;

export interface Toast {
  id: ToastId;
  message: string;
  type: ToastType;
}

interface ToastState {
  toasts: Toast[];
  show: (message: string, type: ToastType) => ToastId;
  update: (id: ToastId, message: string, type?: ToastType) => void;
  dismiss: (id: ToastId) => void;
}

const MAX_TOASTS = 5;

let counter = 0;
function nextId(): ToastId {
  counter += 1;
  return `toast-${Date.now()}-${counter}`;
}

export const useToastStore = create<ToastState>((set) => ({
  toasts: [],
  show: (message, type) => {
    const id = nextId();
    set((state) => {
      const next = [...state.toasts, { id, message, type }];
      // Кепка: при превышении вытесняем старейшие
      const trimmed = next.length > MAX_TOASTS ? next.slice(next.length - MAX_TOASTS) : next;
      return { toasts: trimmed };
    });
    return id;
  },
  update: (id, message, type) =>
    set((state) => ({
      toasts: state.toasts.map((t) => (t.id === id ? { ...t, message, type: type ?? t.type } : t)),
    })),
  dismiss: (id) => set((state) => ({ toasts: state.toasts.filter((t) => t.id !== id) })),
}));
```

- [ ] **Step 4: Создать хук useToast.ts**

```ts
// src/hooks/useToast.ts
import { useToastStore } from '../stores/toastStore';

export function useToast() {
  return {
    show: useToastStore((s) => s.show),
    update: useToastStore((s) => s.update),
    dismiss: useToastStore((s) => s.dismiss),
  };
}
```

- [ ] **Step 5: Запустить тесты**

Run: `npm test -- src/test/toast.test.ts`
Expected: 4 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/stores/toastStore.ts src/hooks/useToast.ts src/test/toast.test.ts
git commit -m "feat(toast): Zustand-стор + хук useToast"
```

---

## Task 9: Toast UI компонент

**Files:**
- Create: `src/components/Toast/Toast.tsx`
- Create: `src/components/Toast/ToastContainer.tsx`
- Create: `src/components/Toast/toast.css`
- Modify: `src/main.tsx`

- [ ] **Step 1: ToastContainer.tsx**

```tsx
// src/components/Toast/ToastContainer.tsx
import { useEffect } from 'react';
import { useToastStore, type Toast as ToastType } from '../../stores/toastStore';
import './toast.css';

const AUTO_DISMISS_MS = { success: 2000, info: 4000 } as const;

function Toast({ toast }: { toast: ToastType }) {
  const dismiss = useToastStore((s) => s.dismiss);

  useEffect(() => {
    if (toast.type === 'success' || toast.type === 'info') {
      const ms = AUTO_DISMISS_MS[toast.type];
      const timer = setTimeout(() => dismiss(toast.id), ms);
      return () => clearTimeout(timer);
    }
    return undefined;
  }, [toast.id, toast.type, dismiss]);

  return (
    <div className={`toast toast-${toast.type}`} role="status">
      {toast.type === 'loading' && <span className="toast-spinner" aria-hidden />}
      <span className="toast-message">{toast.message}</span>
      {(toast.type === 'error' || toast.type === 'loading') && (
        <button
          className="toast-close"
          onClick={() => dismiss(toast.id)}
          aria-label="Закрыть"
        >
          ✕
        </button>
      )}
    </div>
  );
}

export function ToastContainer() {
  const toasts = useToastStore((s) => s.toasts);
  return (
    <div className="toast-container">
      {toasts.map((toast) => (
        <Toast key={toast.id} toast={toast} />
      ))}
    </div>
  );
}
```

- [ ] **Step 2: toast.css**

```css
/* src/components/Toast/toast.css */
.toast-container {
  position: fixed;
  bottom: 16px;
  right: 16px;
  display: flex;
  flex-direction: column;
  gap: 8px;
  z-index: 1200;
  pointer-events: none;
}

.toast {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  min-width: 220px;
  max-width: 360px;
  background: var(--bg-secondary);
  color: var(--text-primary);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  font-size: 13px;
  pointer-events: auto;
  animation: toast-slide-in 150ms ease-out;
}

@keyframes toast-slide-in {
  from { transform: translateY(8px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

.toast-loading {
  border-left: 3px solid var(--accent);
}

.toast-success {
  border-left: 3px solid #2eb872;
}

.toast-error {
  border-left: 3px solid #d83b3b;
}

.toast-info {
  border-left: 3px solid var(--text-muted);
}

.toast-spinner {
  width: 14px;
  height: 14px;
  border: 2px solid var(--text-muted);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: toast-spin 0.8s linear infinite;
}

@keyframes toast-spin {
  to { transform: rotate(360deg); }
}

.toast-message {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.toast-close {
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  font-size: 14px;
  padding: 2px 6px;
}

.toast-close:hover {
  color: var(--text-primary);
}
```

- [ ] **Step 3: Подключить ToastContainer в main.tsx**

Изменить `src/main.tsx`:

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import { EditorProvider } from './components/Editor/EditorContext';
import { ToastContainer } from './components/Toast/ToastContainer';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <EditorProvider>
      <App />
      <ToastContainer />
    </EditorProvider>
  </React.StrictMode>,
);
```

- [ ] **Step 4: Smoke-тест в dev**

Run: `npm run tauri dev` (запустит окно).
Открыть DevTools → Console → выполнить:
```js
window.dispatchEvent(new CustomEvent('mivra-test-toast'));
```
(см. Step 5 — добавить временный listener для smoke OR просто перейти к следующему task.)

Лучше пропустить интерактивный smoke и положиться на интеграционные проверки в следующих task'ах. Закрыть dev-окно.

- [ ] **Step 5: Tsc + tests**

Run: `npx tsc --noEmit`
Run: `npm test`
Expected: всё зелёное.

- [ ] **Step 6: Commit**

```bash
git add src/components/Toast/ src/main.tsx
git commit -m "feat(toast): UI компонент + ToastContainer в main.tsx"
```

---

## Task 10: appStore + setS3Config + загрузка из settings

**Files:**
- Modify: `src/stores/appStore.ts`
- Modify: `src/types/index.ts`
- Modify: `src/hooks/useSettings.ts`

- [ ] **Step 1: Добавить s3 в AppState**

В `src/types/index.ts` обновить `AppState`:

```ts
import type { Settings, FileData, S3Config } from '../bindings';

export type { Settings, FileData, S3Config };

// ... existing fields

export interface AppState {
  // ... все существующие поля
  s3: S3Config | null;
  // Существующие действия + новое:
  setS3Config: (config: S3Config | null) => void;
}
```

- [ ] **Step 2: Обновить appStore.ts**

В `src/stores/appStore.ts`:

```ts
// в initial state:
s3: null,

// в actions:
setS3Config: (s3) => set({ s3 }),

// в updateSettings:
updateSettings: (settings: Partial<Settings>) => set((state) => ({
  fontFamily: settings.font_family ?? state.fontFamily,
  fontSize: settings.font_size ?? state.fontSize,
  theme: (settings.theme as Theme | undefined) ?? state.theme,
  language: (settings.language as Language | undefined) ?? state.language,
  recentFiles: settings.recent_files ?? state.recentFiles,
  pageWidth: settings.page_width ?? state.pageWidth,
  // новое:
  s3: settings.s3 ?? state.s3,
})),
```

- [ ] **Step 3: useSettings.persist должен включать s3**

В `src/hooks/useSettings.ts` обновить функцию `persist`:

```ts
const s3 = useAppStore((s) => s.s3);

const persist = useCallback(async () => {
  try {
    await tauri.writeSettings({
      font_family: fontFamily,
      font_size: fontSize,
      theme,
      language,
      recent_files: recentFiles,
      page_width: pageWidth,
      s3,
    });
  } catch (e) {
    console.error('Ошибка сохранения настроек:', e);
  }
}, [fontFamily, fontSize, theme, language, recentFiles, pageWidth, s3]);

// Также добавить s3 в зависимости автосейв-эффекта:
useEffect(() => {
  const timeout = setTimeout(() => { persist(); }, 500);
  return () => clearTimeout(timeout);
}, [fontFamily, fontSize, theme, language, pageWidth, s3, persist]);
```

- [ ] **Step 4: Tsc + tests**

Run: `npx tsc --noEmit`
Run: `npm test`
Expected: зелёное. Существующие тесты appStore/useSettings продолжают проходить.

- [ ] **Step 5: Commit**

```bash
git add src/stores/appStore.ts src/types/index.ts src/hooks/useSettings.ts
git commit -m "feat(s3): s3 в AppState + setS3Config + автосейв в settings.json"
```

---

## Task 11: useS3Upload хук

**Files:**
- Create: `src/hooks/useS3Upload.ts`
- Create: `src/test/useS3Upload.test.ts`

- [ ] **Step 1: Failing-тесты**

`src/test/useS3Upload.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useS3Upload } from '../hooks/useS3Upload';
import { useAppStore } from '../stores/appStore';
import { useToastStore } from '../stores/toastStore';
import { invoke } from '@tauri-apps/api/core';

const mockedInvoke = vi.mocked(invoke);

describe('useS3Upload', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    useAppStore.setState({ s3: null });
    useToastStore.setState({ toasts: [] });
  });

  it('ready=false при отсутствии s3 в state', async () => {
    mockedInvoke.mockResolvedValue({ status: 'ok', data: false });
    const { result } = renderHook(() => useS3Upload());
    // дать эффекту проверки secret_exists завершиться
    await act(() => Promise.resolve());
    expect(result.current.ready).toBe(false);
  });

  it('ready=true когда s3 настроен и secret существует', async () => {
    useAppStore.setState({
      s3: {
        endpoint: 'https://s3.test',
        region: 'r1',
        bucket: 'b',
        access_key_id: 'k',
        public_url_prefix: null,
        path_prefix: null,
      },
    });
    mockedInvoke.mockResolvedValue({ status: 'ok', data: true });

    const { result } = renderHook(() => useS3Upload());
    await act(() => Promise.resolve());
    expect(result.current.ready).toBe(true);
  });

  it('uploadAndInsertBytes вызывает s3_upload_bytes IPC', async () => {
    useAppStore.setState({
      s3: {
        endpoint: 'https://s3.test', region: 'r1', bucket: 'b',
        access_key_id: 'k', public_url_prefix: null, path_prefix: null,
      },
    });
    // первый вызов — secret_exists, второй — upload_bytes
    mockedInvoke
      .mockResolvedValueOnce({ status: 'ok', data: true })
      .mockResolvedValueOnce({ status: 'ok', data: 'https://s3.test/b/file.png' });

    const onInsert = vi.fn();
    const { result } = renderHook(() => useS3Upload(onInsert));
    await act(() => Promise.resolve());

    await act(async () => {
      await result.current.uploadAndInsertBytes(new Uint8Array([1, 2, 3]), 'test.png');
    });

    const calls = mockedInvoke.mock.calls;
    const uploadCall = calls.find(([cmd]) => cmd === 's3_upload_bytes');
    expect(uploadCall).toBeDefined();
    expect(onInsert).toHaveBeenCalledWith('test', 'https://s3.test/b/file.png', true);
  });

  it('IPC ошибка показывает toast.error', async () => {
    useAppStore.setState({
      s3: {
        endpoint: 'https://s3.test', region: 'r1', bucket: 'b',
        access_key_id: 'k', public_url_prefix: null, path_prefix: null,
      },
    });
    mockedInvoke
      .mockResolvedValueOnce({ status: 'ok', data: true })
      .mockResolvedValueOnce({ status: 'error', error: 'auth_failed: ...' });

    const onInsert = vi.fn();
    const { result } = renderHook(() => useS3Upload(onInsert));
    await act(() => Promise.resolve());

    await act(async () => {
      await result.current.uploadAndInsertBytes(new Uint8Array([1]), 'x.png');
    });

    expect(onInsert).not.toHaveBeenCalled();
    const toasts = useToastStore.getState().toasts;
    expect(toasts.some((t) => t.type === 'error')).toBe(true);
  });
});
```

- [ ] **Step 2: Запустить — fail**

Run: `npm test -- src/test/useS3Upload.test.ts`
Expected: FAIL «Cannot find module ../hooks/useS3Upload».

- [ ] **Step 3: Имплементировать useS3Upload**

```ts
// src/hooks/useS3Upload.ts
import { useCallback, useEffect, useState } from 'react';
import { useAppStore } from '../stores/appStore';
import * as tauri from '../utils/tauri';
import { useToast } from './useToast';

const IMG_EXTS = new Set([
  'png', 'jpg', 'jpeg', 'gif', 'webp', 'svg', 'bmp',
  'apng', 'avif', 'tiff', 'tif', 'heic',
]);
const DOC_EXTS = new Set(['pdf', 'zip', 'mp4', 'webm']);
const SIZE_WARN = 10 * 1024 * 1024;
const SIZE_HARD = 100 * 1024 * 1024;

export type InsertCallback = (name: string, url: string, isImage: boolean) => void;

function extensionOf(name: string): string {
  const i = name.lastIndexOf('.');
  return i >= 0 ? name.slice(i + 1).toLowerCase() : '';
}

function isImage(name: string): boolean {
  return IMG_EXTS.has(extensionOf(name));
}

function isAccepted(name: string): boolean {
  const ext = extensionOf(name);
  return IMG_EXTS.has(ext) || DOC_EXTS.has(ext);
}

function nameWithoutExt(name: string): string {
  const i = name.lastIndexOf('.');
  return i >= 0 ? name.slice(0, i) : name;
}

export function useS3Upload(onInsert?: InsertCallback) {
  const s3 = useAppStore((s) => s.s3);
  const [secretExists, setSecretExists] = useState(false);
  const toast = useToast();

  useEffect(() => {
    let cancelled = false;
    if (!s3) {
      setSecretExists(false);
      return undefined;
    }
    tauri.s3SecretExists()
      .then((exists) => { if (!cancelled) setSecretExists(exists); })
      .catch(() => { if (!cancelled) setSecretExists(false); });
    return () => { cancelled = true; };
  }, [s3]);

  const ready = !!s3 && secretExists;

  const uploadAndInsertBytes = useCallback(
    async (bytes: Uint8Array, originalFilename: string) => {
      if (!s3 || !secretExists) {
        toast.show('S3 не настроен. Откройте настройки.', 'info');
        return;
      }
      if (!isAccepted(originalFilename)) {
        toast.show(`${originalFilename}: расширение не поддерживается`, 'error');
        return;
      }
      if (bytes.length > SIZE_HARD) {
        toast.show(`${originalFilename}: файл больше 100 MB`, 'error');
        return;
      }
      const id = toast.show(`Загрузка ${originalFilename}...`, 'loading');
      try {
        const url = await tauri.s3UploadBytes(bytes, originalFilename, s3);
        toast.update(id, 'Готово', 'success');
        onInsert?.(nameWithoutExt(originalFilename), url, isImage(originalFilename));
      } catch (e) {
        toast.update(id, `Ошибка: ${e}`, 'error');
      }
    },
    [s3, secretExists, toast, onInsert],
  );

  const uploadAndInsertFile = useCallback(
    async (localPath: string, originalFilename: string, sizeBytes?: number) => {
      if (!s3 || !secretExists) {
        toast.show('S3 не настроен. Откройте настройки.', 'info');
        return;
      }
      if (!isAccepted(originalFilename)) {
        toast.show(`${originalFilename}: расширение не поддерживается`, 'error');
        return;
      }
      if (sizeBytes !== undefined && sizeBytes > SIZE_HARD) {
        toast.show(`${originalFilename}: файл больше 100 MB`, 'error');
        return;
      }
      if (sizeBytes !== undefined && sizeBytes > SIZE_WARN) {
        const mb = (sizeBytes / 1024 / 1024).toFixed(1);
        if (!window.confirm(`Загрузить ${originalFilename} (${mb} MB)?`)) return;
      }
      const id = toast.show(`Загрузка ${originalFilename}...`, 'loading');
      try {
        const url = await tauri.s3UploadFile(localPath, originalFilename, s3);
        toast.update(id, 'Готово', 'success');
        onInsert?.(nameWithoutExt(originalFilename), url, isImage(originalFilename));
      } catch (e) {
        toast.update(id, `Ошибка: ${e}`, 'error');
      }
    },
    [s3, secretExists, toast, onInsert],
  );

  return { ready, uploadAndInsertBytes, uploadAndInsertFile };
}
```

- [ ] **Step 4: Запустить тесты**

Run: `npm test -- src/test/useS3Upload.test.ts`
Expected: 4 PASS.

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useS3Upload.ts src/test/useS3Upload.test.ts
git commit -m "feat(s3): useS3Upload хук с whitelist + size + toast"
```

---

## Task 12: i18n строки RU/EN

**Files:**
- Modify: `src/i18n/ru.ts`
- Modify: `src/i18n/en.ts`

- [ ] **Step 1: Добавить ключи в ru.ts**

В `src/i18n/ru.ts` рядом с существующими блоками добавить:

```ts
  // S3
  s3Button: 'S3',
  s3DialogTitle: 'Настройки S3',
  s3Endpoint: 'Endpoint URL',
  s3EndpointHint: 'Например: https://storage.yandexcloud.net',
  s3Region: 'Регион',
  s3RegionHint: 'Например: ru-central1',
  s3Bucket: 'Bucket',
  s3AccessKeyId: 'Access Key ID',
  s3AccessKeySecret: 'Access Key Secret',
  s3SecretSaved: '••• сохранён',
  s3SecretClear: 'Очистить',
  s3PublicUrlPrefix: 'Public URL prefix (опционально)',
  s3PublicUrlPrefixHint: 'Если пусто — авто из endpoint и bucket',
  s3PathPrefix: 'Path prefix (опционально)',
  s3PathPrefixHint: 'Например: mivra/',
  s3TestConnection: 'Тест соединения',
  s3TestSuccess: 'Соединение работает',
  s3TestFail: 'Не удалось подключиться',
  s3SaveSettings: 'Сохранить',
  s3CancelSettings: 'Отмена',
  s3UploadStart: 'Загрузка',
  s3UploadDone: 'Готово',
  s3UploadError: 'Ошибка',
  s3NotConfigured: 'S3 не настроен. Откройте настройки.',
  s3ConfigureForPaste: 'Настройте S3, чтобы вставлять из буфера',
  s3FileTooLarge: 'файл больше 100 MB',
  s3UnsupportedExt: 'расширение не поддерживается',
  s3SizeWarnConfirm: 'Загрузить {name} ({size})?',
```

- [ ] **Step 2: Зеркалить в en.ts**

В `src/i18n/en.ts` те же ключи на английском:

```ts
  // S3
  s3Button: 'S3',
  s3DialogTitle: 'S3 Settings',
  s3Endpoint: 'Endpoint URL',
  s3EndpointHint: 'For example: https://s3.amazonaws.com',
  s3Region: 'Region',
  s3RegionHint: 'For example: us-east-1',
  s3Bucket: 'Bucket',
  s3AccessKeyId: 'Access Key ID',
  s3AccessKeySecret: 'Access Key Secret',
  s3SecretSaved: '••• saved',
  s3SecretClear: 'Clear',
  s3PublicUrlPrefix: 'Public URL prefix (optional)',
  s3PublicUrlPrefixHint: 'If empty, auto-derived from endpoint and bucket',
  s3PathPrefix: 'Path prefix (optional)',
  s3PathPrefixHint: 'For example: mivra/',
  s3TestConnection: 'Test connection',
  s3TestSuccess: 'Connection works',
  s3TestFail: 'Connection failed',
  s3SaveSettings: 'Save',
  s3CancelSettings: 'Cancel',
  s3UploadStart: 'Uploading',
  s3UploadDone: 'Done',
  s3UploadError: 'Error',
  s3NotConfigured: 'S3 is not configured. Open settings.',
  s3ConfigureForPaste: 'Configure S3 to paste from clipboard',
  s3FileTooLarge: 'file is larger than 100 MB',
  s3UnsupportedExt: 'unsupported extension',
  s3SizeWarnConfirm: 'Upload {name} ({size})?',
```

- [ ] **Step 3: Tsc**

Run: `npx tsc --noEmit`
Expected: зелёное (типы синхронизируются автоматически через `Translations = typeof ru`).

- [ ] **Step 4: Commit**

```bash
git add src/i18n/ru.ts src/i18n/en.ts
git commit -m "feat(s3): i18n строки для S3-настроек, диалога и toast"
```

---

## Task 13: S3SettingsDialog component

**Files:**
- Create: `src/components/S3Settings/S3SettingsDialog.tsx`
- Create: `src/components/S3Settings/s3-settings.css`

- [ ] **Step 1: Создать S3SettingsDialog.tsx**

```tsx
// src/components/S3Settings/S3SettingsDialog.tsx
import { useEffect, useState } from 'react';
import { useAppStore } from '../../stores/appStore';
import { getTranslations } from '../../i18n';
import * as tauri from '../../utils/tauri';
import type { S3Config } from '../../bindings';
import { useToast } from '../../hooks/useToast';
import './s3-settings.css';

interface Props {
  onClose: () => void;
}

const EMPTY_CONFIG: S3Config = {
  endpoint: '',
  region: '',
  bucket: '',
  access_key_id: '',
  public_url_prefix: null,
  path_prefix: null,
};

export function S3SettingsDialog({ onClose }: Props) {
  const language = useAppStore((s) => s.language);
  const currentS3 = useAppStore((s) => s.s3);
  const setS3Config = useAppStore((s) => s.setS3Config);
  const t = getTranslations(language);
  const toast = useToast();

  const [form, setForm] = useState<S3Config>(currentS3 ?? EMPTY_CONFIG);
  const [secretInput, setSecretInput] = useState('');
  const [secretExists, setSecretExists] = useState(false);
  const [testing, setTesting] = useState(false);

  useEffect(() => {
    tauri.s3SecretExists().then(setSecretExists).catch(() => setSecretExists(false));
  }, []);

  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    document.addEventListener('keydown', onKey);
    return () => document.removeEventListener('keydown', onKey);
  }, [onClose]);

  const handleField = (key: keyof S3Config) => (e: React.ChangeEvent<HTMLInputElement>) => {
    const value = e.target.value;
    setForm((prev) => ({
      ...prev,
      [key]: key === 'public_url_prefix' || key === 'path_prefix' ? (value || null) : value,
    }));
  };

  const handleClearSecret = async () => {
    try {
      await tauri.s3ClearSecret();
      setSecretExists(false);
      toast.show(t.s3SecretClear, 'success');
    } catch (e) {
      toast.show(`${e}`, 'error');
    }
  };

  const handleTest = async () => {
    setTesting(true);
    try {
      // Если в форме введён новый secret — сначала сохраняем его временно
      if (secretInput) {
        await tauri.s3SetSecret(secretInput);
        setSecretExists(true);
      }
      await tauri.s3TestConnection(form);
      toast.show(t.s3TestSuccess, 'success');
    } catch (e) {
      toast.show(`${t.s3TestFail}: ${e}`, 'error');
    } finally {
      setTesting(false);
    }
  };

  const handleSave = async () => {
    try {
      if (secretInput) {
        await tauri.s3SetSecret(secretInput);
      }
      // Минимальная валидация — endpoint/region/bucket/access_key_id обязательны
      if (!form.endpoint || !form.region || !form.bucket || !form.access_key_id) {
        toast.show('Заполните обязательные поля', 'error');
        return;
      }
      setS3Config(form);
      toast.show('Настройки сохранены', 'success');
      onClose();
    } catch (e) {
      toast.show(`${e}`, 'error');
    }
  };

  return (
    <div className="s3-overlay" onClick={onClose}>
      <div
        className="s3-dialog"
        role="dialog"
        aria-modal="true"
        onClick={(e) => e.stopPropagation()}
      >
        <h2 className="s3-title">{t.s3DialogTitle}</h2>

        <label className="s3-field">
          <span>{t.s3Endpoint}</span>
          <input
            type="text"
            value={form.endpoint}
            onChange={handleField('endpoint')}
            placeholder={t.s3EndpointHint}
          />
        </label>

        <label className="s3-field">
          <span>{t.s3Region}</span>
          <input
            type="text"
            value={form.region}
            onChange={handleField('region')}
            placeholder={t.s3RegionHint}
          />
        </label>

        <label className="s3-field">
          <span>{t.s3Bucket}</span>
          <input type="text" value={form.bucket} onChange={handleField('bucket')} />
        </label>

        <label className="s3-field">
          <span>{t.s3AccessKeyId}</span>
          <input
            type="text"
            value={form.access_key_id}
            onChange={handleField('access_key_id')}
          />
        </label>

        <label className="s3-field">
          <span>{t.s3AccessKeySecret}</span>
          <div className="s3-secret-row">
            <input
              type="password"
              value={secretInput}
              onChange={(e) => setSecretInput(e.target.value)}
              placeholder={secretExists ? t.s3SecretSaved : ''}
            />
            {secretExists && (
              <button type="button" className="s3-btn s3-btn-ghost" onClick={handleClearSecret}>
                {t.s3SecretClear}
              </button>
            )}
          </div>
        </label>

        <label className="s3-field">
          <span>{t.s3PublicUrlPrefix}</span>
          <input
            type="text"
            value={form.public_url_prefix ?? ''}
            onChange={handleField('public_url_prefix')}
            placeholder={t.s3PublicUrlPrefixHint}
          />
        </label>

        <label className="s3-field">
          <span>{t.s3PathPrefix}</span>
          <input
            type="text"
            value={form.path_prefix ?? ''}
            onChange={handleField('path_prefix')}
            placeholder={t.s3PathPrefixHint}
          />
        </label>

        <div className="s3-actions">
          <button
            type="button"
            className="s3-btn s3-btn-ghost"
            onClick={handleTest}
            disabled={testing}
          >
            {testing ? '...' : t.s3TestConnection}
          </button>
          <button type="button" className="s3-btn s3-btn-primary" onClick={handleSave}>
            {t.s3SaveSettings}
          </button>
          <button type="button" className="s3-btn" onClick={onClose}>
            {t.s3CancelSettings}
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: s3-settings.css**

```css
/* src/components/S3Settings/s3-settings.css */
.s3-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1100;
  backdrop-filter: blur(2px);
}

.s3-dialog {
  background: var(--bg-secondary);
  color: var(--text-primary);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow);
  padding: 20px 24px;
  width: min(560px, 92%);
  max-height: 85vh;
  overflow-y: auto;
  font-family: var(--font-family);
}

.s3-title {
  margin: 0 0 16px;
  font-size: 18px;
  font-weight: 600;
}

.s3-field {
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-bottom: 12px;
}

.s3-field span {
  font-size: 12px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.s3-field input {
  padding: 8px 10px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--bg-primary);
  color: var(--text-primary);
  font-family: var(--font-family);
  font-size: 13px;
}

.s3-field input::placeholder {
  color: var(--text-muted);
}

.s3-secret-row {
  display: flex;
  gap: 8px;
}

.s3-secret-row input { flex: 1; }

.s3-actions {
  display: flex;
  gap: 8px;
  justify-content: flex-end;
  margin-top: 20px;
}

.s3-btn {
  padding: 8px 16px;
  border: 1px solid var(--border);
  background: var(--bg-tertiary);
  color: var(--text-primary);
  border-radius: var(--radius);
  cursor: pointer;
  font: inherit;
  font-size: 13px;
}

.s3-btn:hover { background: var(--hover-bg); }
.s3-btn:disabled { opacity: 0.5; cursor: wait; }

.s3-btn-primary {
  background: var(--accent);
  color: var(--accent-text);
  border-color: var(--accent);
}

.s3-btn-primary:hover { background: var(--accent-hover); }

.s3-btn-ghost {
  background: transparent;
}
```

- [ ] **Step 3: Tsc**

Run: `npx tsc --noEmit`
Expected: зелёное.

- [ ] **Step 4: Commit**

```bash
git add src/components/S3Settings/
git commit -m "feat(s3): S3SettingsDialog с формой, test connection, save"
```

---

## Task 14: Toolbar S3 button

**Files:**
- Modify: `src/components/Toolbar/Toolbar.tsx`

- [ ] **Step 1: Добавить кнопку S3 левее «Справка»**

В `src/components/Toolbar/Toolbar.tsx`:

```tsx
// добавить импорт:
import { S3SettingsDialog } from '../S3Settings/S3SettingsDialog';

// в начало компонента:
const [showS3, setShowS3] = useState(false);

// в JSX, в группе со «Справкой» — кнопка ПЕРЕД <button> справки:
<button
  className="toolbar-btn"
  onClick={() => setShowS3(true)}
>
  {t.s3Button}
</button>

// в return после </div> и перед {showHelp && ...}:
{showS3 && <S3SettingsDialog onClose={() => setShowS3(false)} />}
```

- [ ] **Step 2: Smoke в dev**

Run: `npm run tauri dev`
Открыть приложение → нажать «S3» → должен появиться модал. Закрыть.

- [ ] **Step 3: Tsc + tests**

Run: `npx tsc --noEmit`
Run: `npm test`
Expected: зелёное.

- [ ] **Step 4: Commit**

```bash
git add src/components/Toolbar/Toolbar.tsx
git commit -m "feat(s3): кнопка S3 в Toolbar открывает S3SettingsDialog"
```

---

## Task 15: useMarkdownActions интеграция S3 для кнопки

**Files:**
- Modify: `src/hooks/useMarkdownActions.ts`

- [ ] **Step 1: Расширить insertAssetAction**

В `src/hooks/useMarkdownActions.ts` обновить:

```ts
import { open as openDialog } from '@tauri-apps/plugin-dialog';
import { stat } from '@tauri-apps/plugin-fs';
import { useS3Upload } from './useS3Upload';

// внутри useMarkdownActions:
const insertImageOrLink = useCallback(
  (name: string, url: string, isImage: boolean) => {
    const text = isImage ? `![${name}](${url})\n` : `[${name}](${url})\n`;
    applyMarkdownAction({ type: 'insert', text, inline: false });
  },
  [applyMarkdownAction],
);

const s3 = useS3Upload(insertImageOrLink);

const insertAssetAction = useCallback(async () => {
  if (s3.ready) {
    const selected = await openDialog({
      multiple: false,
      directory: false,
      filters: [{
        name: 'Assets',
        extensions: [
          'png', 'jpg', 'jpeg', 'gif', 'webp', 'svg', 'bmp',
          'apng', 'avif', 'tiff', 'tif', 'heic',
          'pdf', 'zip', 'mp4', 'webm',
        ],
      }],
    });
    if (!selected || typeof selected !== 'string') return;
    const filename = selected.split(/[\\/]/).pop() ?? selected;
    let size: number | undefined;
    try {
      const meta = await stat(selected);
      size = meta.size;
    } catch {
      size = undefined;
    }
    await s3.uploadAndInsertFile(selected, filename, size);
    return;
  }

  // S3 не настроен — старый локальный pickAndFormatAsset
  try {
    const md = await pickAndFormatAsset(baseDir);
    if (md) {
      applyMarkdownAction({ type: 'insert', text: md, inline: false });
    }
  } catch (e) {
    console.warn('[useMarkdownActions] insertAsset cancelled or error:', e);
  }
}, [s3, applyMarkdownAction, baseDir]);
```

- [ ] **Step 2: Capabilities — fs:allow-stat если ещё не разрешено**

В `src-tauri/capabilities/default.json` проверить наличие, если нет — добавить:

```json
{
  "identifier": "fs:allow-stat",
  "allow": [
    { "path": "$HOME/**" },
    { "path": "$DESKTOP/**" },
    { "path": "$DOCUMENT/**" },
    { "path": "$DOWNLOAD/**" }
  ]
}
```

- [ ] **Step 3: Tsc + tests + cargo check**

Run: `npx tsc --noEmit`
Run: `npm test`
Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Expected: всё зелёное.

- [ ] **Step 4: Commit**

```bash
git add src/hooks/useMarkdownActions.ts src-tauri/capabilities/default.json
git commit -m "feat(s3): кнопка Toolbar/Ctrl+Shift+A через S3 когда настроен"
```

---

## Task 16: Drag & drop handler

**Files:**
- Modify: `src/components/Editor/Editor.tsx`

- [ ] **Step 1: Подключить Tauri drag-drop event**

В `src/components/Editor/Editor.tsx` добавить:

```tsx
import { getCurrentWindow, type DragDropEvent } from '@tauri-apps/api/window';
import { stat } from '@tauri-apps/plugin-fs';
import { useS3Upload } from '../../hooks/useS3Upload';
```

Внутри компонента после существующих `useAppStore` селекторов:

```tsx
// applyMarkdownAction для S3-вставки в текущую позицию курсора
const setContentLocal = useAppStore((s) => s.setContent);

const insertAtCursor = useCallback(
  (name: string, url: string, isImage: boolean) => {
    // Используем тот же flow, что useMarkdownActions:
    // вставка через handleRef.current.editor / sourceTextarea.
    const text = isImage ? `![${name}](${url})\n` : `[${name}](${url})\n`;
    if (editorMode === 'source') {
      const ta = handleRef.current.sourceTextarea;
      if (!ta) return;
      const start = ta.selectionStart ?? ta.value.length;
      const next = ta.value.slice(0, start) + text + ta.value.slice(start);
      setContentLocal(next);
      requestAnimationFrame(() => {
        ta.focus();
        ta.setSelectionRange(start + text.length, start + text.length);
      });
    } else {
      const editor = handleRef.current.editor;
      if (!editor) return;
      // Для visual режима используем простую вставку через setContent — Crepe пересоздастся
      // через content-change effect (уже есть в этом файле). Это OK для вставки URL,
      // потому что пользователь видит результат через ~16мс.
      const current = contentRef.current;
      const insertText = text;
      const newContent = current + (current.endsWith('\n') ? '' : '\n') + insertText;
      setContentLocal(newContent);
    }
  },
  [editorMode, handleRef, setContentLocal],
);

const s3 = useS3Upload(insertAtCursor);
```

И отдельный effect для подписки на drag-drop:

```tsx
useEffect(() => {
  if (!s3.ready) return;
  let unlisten: (() => void) | undefined;
  let cancelled = false;

  const win = getCurrentWindow();
  win.onDragDropEvent(async (event) => {
    if (event.payload.type !== 'drop') return;
    const paths = event.payload.paths;
    for (let i = 0; i < paths.length; i++) {
      const path = paths[i];
      const filename = path.split(/[\\/]/).pop() ?? path;
      let size: number | undefined;
      try { size = (await stat(path)).size; } catch { size = undefined; }
      await s3.uploadAndInsertFile(path, filename, size);
    }
  }).then((u) => {
    if (cancelled) u(); else unlisten = u;
  });

  return () => {
    cancelled = true;
    unlisten?.();
  };
}, [s3]);
```

- [ ] **Step 2: Smoke в dev**

Run: `npm run tauri dev`
- Настроить S3 (или замокать секрет вручную через keyring; либо протестируем после Task 18).
- Дропнуть PNG из проводника в окно. Если S3 настроен — ожидать toast загрузки.
- Если S3 не настроен — ничего не происходит (это by design).

- [ ] **Step 3: Tsc + tests**

Run: `npx tsc --noEmit`
Run: `npm test`
Expected: зелёное.

- [ ] **Step 4: Commit**

```bash
git add src/components/Editor/Editor.tsx
git commit -m "feat(s3): drag&drop файлов в Editor через Tauri onDragDropEvent"
```

---

## Task 17: Paste handler

**Files:**
- Modify: `src/components/Editor/Editor.tsx`

- [ ] **Step 1: Добавить document-level paste listener**

В `src/components/Editor/Editor.tsx` добавить отдельный useEffect:

```tsx
useEffect(() => {
  if (!s3.ready) return;

  const onPaste = async (e: ClipboardEvent) => {
    const items = e.clipboardData?.items;
    if (!items) return;

    for (const item of items) {
      if (item.kind !== 'file') continue;
      if (!item.type.startsWith('image/')) continue;

      const file = item.getAsFile();
      if (!file) continue;

      e.preventDefault();
      e.stopPropagation();

      const buffer = new Uint8Array(await file.arrayBuffer());
      const ext = ({
        'image/png': 'png',
        'image/jpeg': 'jpg',
        'image/gif': 'gif',
        'image/webp': 'webp',
      } as Record<string, string>)[item.type] ?? 'png';
      const filename = `clipboard-${Date.now()}.${ext}`;
      await s3.uploadAndInsertBytes(buffer, filename);
      return;
    }
  };

  document.addEventListener('paste', onPaste, true);
  return () => document.removeEventListener('paste', onPaste, true);
}, [s3]);
```

- [ ] **Step 2: Smoke**

Run: `npm run tauri dev`
- С настроенным S3: Win+Shift+S → выбрать область экрана → Ctrl+V в редакторе → toast загрузки → markdown с URL вставляется.
- Без S3: Crepe должен отработать paste картинки штатно (на нашей стороне просто игнор).

- [ ] **Step 3: Tsc + tests**

Run: `npx tsc --noEmit`
Run: `npm test`
Expected: зелёное.

- [ ] **Step 4: Commit**

```bash
git add src/components/Editor/Editor.tsx
git commit -m "feat(s3): paste картинок из буфера через document paste listener"
```

---

## Task 18: Финальный smoke + регенерация GitNexus

**Files:** —

- [ ] **Step 1: Полный test-suite**

Run: `npm test`
Run: `npx tsc --noEmit`
Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Run: `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings`
Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib s3`

Expected: всё зелёное.

- [ ] **Step 2: Smoke в dev с реальным bucket'ом**

Подготовить тестовый bucket в Yandex Object Storage или TimeWeb:
1. Создать bucket с публичным read access
2. Получить Static Access Key (key id + secret)

Run: `npm run tauri dev`
Проверить:
1. Открыть «S3» → ввести endpoint/region/bucket/access_key_id + Secret → Save.
2. Тест соединения → ✓.
3. Drag & drop PNG из проводника → toast → markdown ![]() с URL → URL открывается в браузере.
4. Win+Shift+S → выделить область → Ctrl+V в редактор → загрузка → ![](url).
5. Кнопка «Вставить файл» → выбрать PDF → toast → [name](url).
6. Очистить Secret → попытка drag&drop → toast info "S3 не настроен".
7. Открыть «S3», стереть Endpoint, Save → drag&drop → toast info.

- [ ] **Step 3: Регенерировать GitNexus индекс**

Run: `npx gitnexus analyze`
Expected: успешно, кол-во nodes/edges увеличилось.

- [ ] **Step 4: Финальный commit (если что-то fixed)**

Если в smoke что-то упало и пришлось править — отдельный fix-commit. Иначе:

```bash
git status   # ожидать "nothing to commit"
```

- [ ] **Step 5: Закрыть фичу**

```bash
git log --oneline main..HEAD
```

Должно быть ~17 commits для S3-фичи. Готовить PR в `main` или мердж по решению.

---

## Self-Review

**1. Spec coverage:**

| Spec section | Task |
|---|---|
| Архитектура (frontend/backend) | Tasks 1, 6, 9, 13 |
| S3Config struct + specta | Tasks 1, 6 |
| IPC контракт (set/clear/exists/test/upload) | Tasks 3, 5, 6 |
| Whitelist + size limits | Task 11 |
| Drag&drop | Task 16 |
| Paste | Task 17 |
| Toolbar button | Tasks 14, 15 |
| Toast UI | Tasks 8, 9 |
| S3SettingsDialog | Task 13 |
| Capabilities (fs:allow-stat) | Task 15 |
| Sanitize + key + URL | Task 2 |
| Retry x3 | Task 4 |
| Test connection | Task 5 |
| keyring | Task 3 |
| i18n | Task 12 |
| appStore.s3 | Task 10 |

Все требования спека покрыты задачами.

**2. Placeholders:** «TBD»/«TODO»/«similar to»/etc. в плане отсутствуют. Проверено.

**3. Type consistency:**
- `S3Config` имена полей (`endpoint`, `region`, `bucket`, `access_key_id`, `public_url_prefix`, `path_prefix`) одинаковы во всех task'ах.
- `useS3Upload` API: `ready`, `uploadAndInsertBytes`, `uploadAndInsertFile` — одинаково в Task 11, 15, 16, 17.
- IPC commands: `s3SetSecret`, `s3ClearSecret`, `s3SecretExists`, `s3TestConnection`, `s3UploadFile`, `s3UploadBytes` — одинаково везде.
- Sanitize/build_key/derive_public_url — функции из Task 2 переиспользуются в Task 4.

OK.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-29-s3-uploads.md`. Two execution options:

**1. Subagent-Driven (recommended)** — я диспатчу свежего субагента на каждую task, между task'ами ревью, быстрая итерация.

**2. Inline Execution** — выполняю task'и в этой сессии через executing-plans, batch с чекпоинтами для ревью.

Какой подход?
